# Lite シェル L0+L1: 純ロジック + ThreadList の optional prop

- プラン: `~/.claude/plans/purrfect-drifting-pascal.md`（read-only 調査 `quizzical-zooming-flute.md` の戦略 B 採択を受けたサイクル）
- 起点: `310b412`（スレッド管理 サイクル4）、`git status -sb` clean 確認済み
- スコープ: `apps/web` + `doc` のみ。`ChatPage.tsx` diff ゼロ、`apps/server`/`agents`/`packages`/`prisma` diff ゼロが目標

## D2 事前確認: pin テストが古い dist に依存して偽陽性にならないか（承認ノートで指示された実測）

`apps/web/tsconfig.test.json` に `incremental` / `composite` / `tsBuildInfoFile` の指定なし
（プレーンな非インクリメンタル compile）。`package.json` の `"pretest": "tsc -p tsconfig.test.json"` は
`pnpm test` 実行時に自動的に先行実行される（npm lifecycle）。

実測手順:
1. 事前の `dist-test/lib/thread-routing-client.js` mtime: `Sep 10 01:57`（前回セッションの古い成果物）
2. ソース無変更のまま `cd apps/web && pnpm test` を実行 → 31 pass（既存件数、ベースライン確認）
3. 実行後の mtime: `2026-09-10 08:09:49.324183233 +0900`（実行直後の壁時計 `08:09:53` と一致）

→ **`tsc -p tsconfig.test.json` はソース差分の有無に関わらず毎回フルコンパイルして `dist-test/` を
上書きする**（インクリメンタルキャッシュによるスキップが起きない設定であることを実測で確認）。
よって F4 pin テスト（`dist-test/lib/thread-routing-client.js` を直接 import）が「古い成果物のおかげで
たまたま通る」偽陽性になる余地はない。**pin テストの前段に追加のビルドステップは不要**と判断（プランどおり）。

## 実装

### 決定事項（実装前にユーザー承認済み、プラン `purrfect-drifting-pascal.md` の「決定事項」節）
- D1: 純モジュールは依頼どおり `src/components/lite/lite-shell-rules.ts`。ただし L1 が必要とする
  `resolveCreateTargetProjectId()` だけは既存 `src/lib/thread-list-rules.ts` に追記（依存方向の逆流回避）
- D2: `shouldRouteToTab` は複製せず、L0 ではソース変更ゼロで pin テストのみ（本ファイル冒頭の実測で
  dist-test が毎回フルコンパイルされることを確認済み。偽陽性の余地なし）
- D3: `displayName` はトリムしない（`displayName ?? name`。コードベース全 16 箇所と揃える）
- D4: `decideProjectChange` は作成を返さない（`select-only` / `noop` のみ）。作成は必ず
  `decideSendAction` の `create-then-send` 経由（`buildThreadCreateRequest` を必ず通り tabId 必須が保証される）

### 新規: `apps/web/src/components/lite/lite-shell-rules.ts`（外部 import ゼロ）
- `buildThreadCreateRequest()` / `ThreadCreateRequest`（R8: tabId 必須の唯一の入口）
- `decideSendAction()`（F1 の 3 ケース: 未選択→create / 別プロジェクト→create / 同一→send-existing。
  blocked 優先順位 `no-project > no-tab-id > offline > disconnected > in-flight > empty`）
- `decideProjectChange()`（D4: create を返さない）
- `resolveThreadProjectView()`（F2/F3 吸収層。`ProjectViewSource` は api.ts の `Project` を import せず
  構造的に最小の型を独自定義）
- `shouldShowApprovalCard()`（R5: fail-open 原則の唯一の例外、`viewSessionId` 無しは false）
- `FORBIDDEN_LITE_BINDINGS` / `FORBIDDEN_LITE_MODULES` / `findForbiddenLiteImports()`（F5 トリップワイヤ。
  namespace import は検出できない既知の限界をテストで明示）

### 追記: `apps/web/src/lib/thread-list-rules.ts`
- `resolveCreateTargetProjectId(projectId?, createProjectId?)` を追加（`??` で導出。両方 undefined なら
  undefined を返すことが L1 の「挙動不変」主張の機械的な証明点）

### 変更: `apps/web/src/components/ThreadList.tsx`（L1・optional prop）
- `ThreadListProps` に `createProjectId?: string` を追加
- `createTargetProjectId = resolveCreateTargetProjectId(projectId, createProjectId)` を導出し、
  `handleCreate` のガード・`threadsApi.create()` 呼び出し・依存配列・`disabled` の **3 箇所 + 依存配列**のみ
  `projectId` → `createTargetProjectId` に差し替え
- 一覧取得（`threadsApi.list(projectId)`）と横断表示バッジ（`{!projectId && ...}`）は**生の `projectId` のまま**
  （`git diff` で確認済み。差し替えたのは意図した 3 箇所だけ）

### 変更: `apps/web/tsconfig.test.json`
- `include` に `"src/components/lite/lite-shell-rules.ts"` を追加

### 新規: `apps/web/tests/lite-shell-rules.test.mjs`（52 件）
F1 の 3 ケース、blocked 優先順位（単独 6 + 優先順位 3）、R3（sendProjectIdHint 一致）、D4 回帰固定、
F2/F3（displayName/machine 解決、トリムしない、fallback）、R5（fail-closed）、F5 トリップワイヤ
（多行 import・as 別名・namespace import の既知の限界）、F4 pin ブロック（既存 `thread-routing-client.js`
を直接 import、ソース変更ゼロ）を含む。

### 追記: `apps/web/tests/thread-list-rules.test.mjs`（4 件）
`resolveCreateTargetProjectId` の優先順位 / フォールバック / 両方 undefined / 空文字（`??` の意味論固定）

## 検証結果

1. `pnpm build` — 6 workspace すべて green（`packages/shared` / `agents/{linux,macos,windows}` /
   `apps/server` / `apps/web`（`tsc -b && vite build`））
2. `cd apps/web && pnpm test` — **83 pass / 0 fail**（baseline 31 → +52、新規テスト分すべて反映され
   silently skip なし）
3. `dist-test/components/lite/lite-shell-rules.js` 生成確認済み（9515 bytes）
4. `grep -c 'require(' apps/web/dist/assets/index-*.js` = **0**（#310 の必須ゲート）
5. `git diff --stat -- apps/web/src/pages/ChatPage.tsx` = **空**（従来 UI 本体の無変更を機械的に証明）
6. `git diff --stat -- apps/server agents packages prisma` = **空**（スコープ逸脱ゼロ）
7. `git diff -- apps/web/src/components/ThreadList.tsx` = optional prop 追加の範囲内
   （14 insertions / 6 deletions、`projectId?` は必須化されていない。list fetch と横断バッジは無変更）

## 変更ファイル一覧
- 新規: `apps/web/src/components/lite/lite-shell-rules.ts`
- 新規: `apps/web/tests/lite-shell-rules.test.mjs`
- 追記: `apps/web/src/lib/thread-list-rules.ts`（1 関数）
- 追記: `apps/web/tests/thread-list-rules.test.mjs`（4 テスト）
- 変更: `apps/web/src/components/ThreadList.tsx`（optional prop、3 箇所 + 依存配列）
- 変更: `apps/web/tsconfig.test.json`（`include` 1 行）
- 新規: `doc/devlog/2026-09-10_080938.md`（本ファイル）
- 追記: `doc/devlog/INDEX.md`（末尾 1 行）

`apps/web/src/pages/ChatPage.tsx` は 0 行。`apps/server` / `agents/` / `packages/` / `prisma/` は無変更。

## 反映
`apps/web` のみの変更 + 呼び出し元ゼロ（`lite-shell-rules.ts` はどこからも import されていない、
`ThreadList` の新規 optional prop は `ChatPage.tsx` から渡されていない）のため、
**server 再起動不要・Agent `u` 不要・DB マイグレーション不要・反映作業不要**。

## 次サイクル
L2（`/lite` ルート + 空シェル）→ L3（読み取り）→ L4（送信）→ L5（承認カード）。
その後、独立サイクルで承認カードの sessionId ゲート（`packages/shared` 型追従を含む）。
