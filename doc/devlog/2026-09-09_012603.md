# 2026-09-09 01:26 JST ｜ サイクルA: Windows agent 純モジュール移植

## 要求
前サイクル（read-only 調査、Plan `memoized-sauteeing-swan.md`）で採択した Option A「Linux 構造に寄せる」の
サブサイクル A を実施。8 モジュール（scope-dir / resume-priority / session-scope / path-mutex /
atomic-write / plan-file-store / sdk-stop-reason / history-compaction）を agents/linux から
agents/windows へ byte-for-byte コピーし、テスト基盤（`node --test`）を追加する。呼び出し元ゼロ・
挙動変更ゼロの純追加のみ。connection.ts / ai-runner.ts / session-store.ts / conversation-store.ts は
一切触らない。

## 実行
- 前サイクル Plan（`memoized-sauteeing-swan.md`）全文を `doc/plans/windows-agent-migration-plan.md` へ無改変コピー
- ベースライン `pnpm --filter @devrelay/agent-windows build` が変更前から green であることを先に確認
- 8 モジュール（`scope-dir.ts`/`resume-priority.ts`/`session-scope.ts`/`path-mutex.ts`/`atomic-write.ts`/
  `plan-file-store.ts`/`sdk-stop-reason.ts`/`history-compaction.ts`）を `cp` 1 コマンドずつで
  `agents/linux/src/services/` → `agents/windows/src/services/` へ byte-for-byte コピー（改変なし）
- `agents/windows/tests/` を新設し、7 テスト（`scope-dir`/`resume-priority`/`session-scope`/
  `atomic-write`/`plan-file-store`/`sdk-stop-reason`/`history-compaction`）を verbatim コピー
- `agents/windows/package.json` の `"build": "tsc",` 直後に `"test": "node --test tests/",` を 1 行追加
  （他行は無変更）

## 発見（重要・依頼の「10 テスト」から 3 本を除外）
依頼文は 10 テストのコピーを指示していたが、前サイクル Plan §8 のサブサイクル A 定義（「8 モジュール+**8**テスト」、
本サイクルでは承認ノートによりさらに 7 本へ調整）と実測に基づき、以下 3 本は **本サイクルではコピーせず
サブサイクル B へ送った**（ユーザー承認済み）:
- `session-store-scope.test.mjs` — windows の `session-store.ts` はフラット構造で `SessionMeta`/4引数
  `saveClaudeSessionId` を持たない（「session-store.ts は一切触らない」という本サイクルの禁止事項と衝突） → B1
- `conversation-store-scope.test.mjs` — windows の `conversation-store.ts` に `agentScopeId` 引数が無い
  （同上、「conversation-store.ts は一切触らない」と衝突） → B2
- `path-mutex.test.mjs` — 実測: `:12` で `dist/services/conversation-store.js` から `mutateConversation`/
  `loadConversation` を import しているが、windows の `conversation-store.ts` は `mutateConversation` を
  **export していない**（export 一覧: `loadConversation`/`saveConversation`/`appendToConversation`/
  `clearConversation`/`archiveConversation`/`markExecPoint`/`getConversationContext` のみ）。
  conversation-store.ts を触らずには green にできないため → B2。
  **`path-mutex.ts` 本体（モジュール）は 8 本の 1 つとして予定どおりコピー済み**（テストのみ先送り）

## 先送り事項（明記）
- **R-A3**: `atomic-write.ts` の Windows `fs.rename` 上書き挙動は実機未検証（`:7-13` にコメントで明記済み）。
  本サイクルではどこからも import されない dormant 状態のため実害ゼロ。実機確認は配線が入るサブサイクル B 以降
- **JSDoc 不変条件の齟齬**: `session-scope.ts:13`/`path-mutex.ts:11`/`atomic-write.ts:17` の
  「linux と macos で byte-for-byte 同一」という記述は、本サイクル後 windows も同一になったため実質不正確
  （3 OS で同一になった）。修正には `agents/linux/`・`agents/macos/` の同 1 行も同時に直す必要があり、
  本サイクルの変更範囲（`agents/windows/` + `doc/` のみ）と「コピーしたファイルへの改変禁止」の両方に反する
  ため実施せず、**サブサイクル B で 3 OS 同時に修正**する

## 検証
- **byte 一致**: `md5sum` で 8 モジュール + 7 テストの計 15 ペア、全ペア一致を確認
- **型チェック**: `cd agents/windows && npx tsc --noEmit` → エラー 0
- **ビルド**: `pnpm --filter @devrelay/agent-windows build` → green。`dist/services/` に 8 本の
  `.js`/`.d.ts`/`.map` が出力されていることを確認
- **windows テスト**: `pnpm --filter @devrelay/agent-windows test` → **pass 72 / fail 0**（新規追加分含む全数）
- **linux 退行なし**: `pnpm --filter @devrelay/agent test` → **pass 438 / fail 0**（本サイクルで linux は
  一切変更していないため当然だが実測で確認）
- **スコープ確認**: `git status --short` は `agents/windows/` 配下（新規8ファイル+7テスト+package.json変更）と
  `doc/`（devlog本体・INDEX・plans）のみ。`git diff --stat -- apps/ packages/ prisma/ agents/linux/ agents/macos/`
  は空
- **未配線の機械的証明**: `import ... from './(8モジュール名)'` 形式の grep が windows/src 全体で **0 件**。
  単純パターン一致では `devin-atif.ts`/`git-guard.ts`/`git-guard-core.ts`/`devin-file-watch.ts` の
  既存コメント（他ファイルの命名パターンへの言及）がヒットしたが、`git diff --stat` でこの 4 ファイルへの
  変更が皆無であることを確認済み（コメントは移植前から存在）
- `connection.ts`/`ai-runner.ts`/`session-store.ts`/`conversation-store.ts` への `git diff --stat` は
  すべて空（無変更）

## 移植元コミットハッシュ
`9c49644`（#376: scope-dir/resume-priority/session-store/conversation-store）、
`8c1dc69`（#377: sdk-stop-reason）、`a6f0e3f`（#348: path-mutex/atomic-write/session-scope）、
`4e2bcae`（#372: history-compaction）。#375（plan-file-store）は `9c49644` の後続で導入。

## 結論
8 モジュール・7 テストとも byte-for-byte・windows/linux 双方 green・呼び出し元ゼロを実測で確認。
`connection.ts`/`ai-runner.ts`/`session-store.ts`/`conversation-store.ts` は無変更のまま。
commit + push を実施しサブサイクル B へ引き継ぐ。
