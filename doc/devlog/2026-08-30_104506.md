# core#334 follow-up: 検証で検出した軽微3点の修正 (2026-08-30)

## 要求
直前の read-only 検証（submission `cmtf4e67p00a7db94sbrxgv69`、`vast-stargazing-marble.md` プラン）で
core#334 の実装を10項目チェックした結果、機能不備（❌）は0件だったが以下3点の軽微な不整合が見つかった。
1. ゲート②（チャット `e,<指示>`）の `humanTextMeta` のみ `neutralized` キーが欠落し①③とキー集合が不一致。
2. `doc/changelog.md` の #334 残件に「上限値の実測分布調整」未実施が未記録。
3. `human-text-fence.test.mjs` のテスト件数を「17件」と記載（実際は16件）。

## 実行
1. `apps/server/src/services/command-handler.ts:45` の import に `neutralizeHumanInputTag` を復帰し、
   `:823-830` の `execHumanTextMeta` JSON に `neutralized: neutralizeHumanInputTag(customPrompt).count,`
   を `fenced` の直後・`rawRef` の直前へ追加（①③と同じキー順に統一）。長さ検証位置（`:697-710`）・
   `content: execContent`・fence 本体（`:858-861`）は無変更。
2. `doc/changelog.md:52` の直後（「いずれも別サイクルの課題として残す。」の後）に「また上限値
   （① 2,000 / ② 4,000 / ③ 20,000）は実測分布による調整を行っていない。運用後に `Message.content`
   長の分布を取り、別サイクルで見直す。」を追記。
3. `doc/changelog.md:59` と `MEMORY.md` の該当行の「17件」を「16件」に訂正。
   **`doc/devlog/2026-08-30_092942.md`（前サイクルの devlog）自体の「17件」記載は訂正していない**
   （既存 devlog は確定記録として追記・改変しない運用のため）。ここに記録することで訂正の追跡可能性を
   担保する: 前サイクル devlog `2026-08-30_092942.md:33` の「17件」は誤りで、正しくは **16件**
   （`grep -c '^test(' human-text-fence.test.mjs` = 16、neutralize 6 + fence 6 + validate 4）。

## 検証
- `pnpm build`（`/opt/devrelay` で実行、cwd ドリフト回避のため絶対パス cd）: 6 workspace すべて green
- `cd apps/server && pnpm test`: 24/24 green（サロゲートペアケース含む human-text-fence.test.mjs は16件）
- `cd agents/linux && pnpm test`: 34/34 green（非退行）
- `cd agents/macos && pnpm test`: 34/34 green（非退行）
- `git diff --stat -- agents/ apps/web/`: 空（各マシンの `u` は不要）
- `grep -c 'require(' apps/web/dist/assets/index-*.js`: 0
- `grep -n "neutralized" apps/server/dist/services/command-handler.js`: `neutralized:
  neutralizeHumanInputTag(customPrompt).count,` を確認（ビルド成果物に反映済み）
- 3経路（`command-handler.ts` / `mcp/tools.ts` の submitInstruction・approvalNote）の
  `humanTextMeta` JSON キー集合（kind/origin/rawLength/limit/fenced/neutralized/rawRef）が一致することを
  grep で確認

**pm2 restart は実行していない**（自己改変サイクルの厳守事項）。DB DDL・`npx prisma generate` は不要
（スキーマ変更なし）。`.ts` を変更したため、反映には人間による `pm2 restart devrelay-server` が必要。

## 発見
- 前サイクル（#334本体）は `neutralized` を①③にのみ実装しており②に付け忘れていた（3経路を横展開する
  実装では、コピー元1箇所を直してから残りを機械的に揃えるのではなく、各経路を独立に書いたためキー集合が
  ドリフトした可能性が高い）。今後同種の「3経路横展開」実装では、実装直後に監査 JSON のキー集合を grep
  で突き合わせる一手間が有効（本サイクルの検証項目7で発見できた）。
- devlog の「確定記録は改変しない」運用と「誤記載を訂正したい」という要求が両立しないケースがあることを
  確認。今回は新規 devlog に訂正を明記する方式で解決したが、今後同種の誤記載が繰り返される場合は
  `INDEX.md` に「訂正」欄を設ける等の運用改善を検討してもよい（今回は現行ルールの範囲内で対応し、
  ルール自体の変更はしていない）。
