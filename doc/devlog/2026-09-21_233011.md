# raw-completion Phase 2.1: 軽量席モデルの追加（gpt-5.6-luna / claude-haiku-4-5）

## 背景

dangou-card の実験席に軽量モデルを足したい、という依頼。指示の前提は「Codex 許可リストは
`gpt-5.6-terra`/`gpt-5.6-sol` のみ（Phase 2、commit `6f730dc`）」「Claude 側は `claude-haiku-4-5` が
許可されているか未確認」だった。

## 調査結果：両モデルとも既に許可済みと判明

| 経路 | 許可リストの実体 | 状態 |
|------|------------------|------|
| Codex | `apps/server/src/routes/raw-completion-api.ts:171` → `validateRawCodexModel(model, AI_MODEL_CATALOG.codex.map(m => m.id))` | `packages/shared/src/constants.ts` に `gpt-5.6-luna` が **既に存在**（sol/terra/luna/5.5/5.4/5.4-mini の6件）。Agent 側 `agents/{linux,macos}/src/services/raw-codex-mode.ts` の `safeRawCodexModel()` は危険文字チェックのみで ID 制限なし |
| Claude | **許可リスト自体が存在しない**。`raw-completion-api.ts` は `ai==='codex'` のときだけモデル検証し、Claude は `model` をそのまま `RawPromptPayload.model` → SDK `sdkOptions.model` へ素通し | `claude-haiku-4-5` はそのまま通る（`AI_MODEL_CATALOG.claude` にも存在） |

「terra/sol のみ」という認識は、Phase 2 の changelog/devlog/README の見出し「terra / sol 席」および
README raw-completion 節の「`gpt-5.6-terra`/`gpt-5.6-sol`/etc. from `AI_MODEL_CATALOG.codex`」という
書き方に由来する **ドキュメント上の誤解** であり、コードの制限ではなかった。

稼働中サーバー（pm2 `devrelay-server` 起動 2026-09-21T01:12 JST）は Phase 2 の dist（2026-09-20 23:25 JST
ビルド、commit `6f730dc`）を既に読み込んでいたため、luna/haiku は調査時点で既に実機で叩ける状態だった。

## 変更内容

**サーバーの許可リスト・ロジックは 0 行変更**（constants.ts / raw-completion-ai.ts / raw-completion-api.ts
いずれも無変更、`git diff --stat` で `src/` 配下が完全に 0 変更であることを確認済み）。

1. テスト追加（許可済みの事実を回帰防止として固定）
   - `packages/shared/tests/model-catalog.test.mjs`: `codex` カタログに `gpt-5.6-luna` が存在するテスト、
     sol/terra/luna の3席が揃っているテスト、`claude` カタログに `claude-haiku-4-5` が存在するテストを追加
   - `apps/server/tests/raw-completion-ai.test.mjs`: `validateRawCodexModel('gpt-5.6-luna', ...)` が ok
     になるテストを追加。さらに `@devrelay/shared` の実カタログ（テスト内手書きの `CATALOG_IDS` ではなく
     `AI_MODEL_CATALOG.codex.map(m => m.id)`）で sol/terra/luna が ok になるテストを追加し、手書きリストと
     実カタログの乖離を仕組みで検出できるようにした
2. `README.md` raw-completion 節を更新
   - Codex 側: `model` の説明を「`gpt-5.6-terra`/`gpt-5.6-sol`/etc.」→ 実カタログ全6件を明記
   - Claude 側: 「`model` に server 側の許可リストは無く、SDK にそのまま渡す（例: `claude-haiku-4-5` で
     軽量席）」を追記
3. `doc/changelog.md` に1行追記、本 devlog を新規作成、`doc/devlog/INDEX.md` 末尾に1行追記

## テスト結果

- `pnpm build`: 6 workspace すべて green
- `packages/shared`: 50/50 pass（47 → 50、+3 新規）
- `apps/server`: 525/525 pass（523 → 525、+2 新規）
- `git diff --stat -- packages/shared/src apps/server/src agents/ apps/web/src prisma`: 出力なし（0 変更を確認）

## 実機確認（uso8m / dangou-card プロジェクト、cmslf2jmd046mct0izdjr66qd）

呼び出し元は同一チーム「DevRelay Team」に所属する `x220-158-18-103/devrelay`（本セッションの Agent 自身）の
machine token を使用。

### (l) Codex `gpt-5.6-luna` 席

```bash
curl -sS -w '\nHTTP_STATUS:%{http_code}\n' -X POST https://app.devrelay.io/api/agent/raw-completion \
  -H "Authorization: Bearer <MACHINE_TOKEN>" -H "Content-Type: application/json" \
  -d '{"targetProjectId":"cmslf2jmd046mct0izdjr66qd","seatKey":"dangou-P05-luna","ai":"codex","model":"gpt-5.6-luna",
       "system":"あなたは談合カードのプレイヤー P05 である。JSON のみで応答せよ",
       "prompt":"{\"question\":\"はい/いいえで答えよ: あなたはP05か\"}"}'
```

結果: `HTTP_STATUS:200`

```json
{
  "text": "{\"answer\":\"はい\"}",
  "output": "{\"answer\":\"はい\"}",
  "model": "gpt-5.6-luna",
  "usage": { "input": 9993, "output": 9, "cacheRead": 0, "cacheWrite": 0 },
  "latencyMs": 7997,
  "agentDurationMs": 7973,
  "stopReason": "success",
  "sessionId": "raw_8cf1e059-de4b-4e7d-a6cd-f34dcb3ccd55",
  "deniedTools": [],
  "ai": "codex"
}
```

`model` はリクエスト値のエコー（`gpt-5.6-luna`）。ChatGPT サブスク側で luna モデルが選択でき、
Codex CLI からのエラーは発生しなかった。`latencyMs=7997`。

### (h) Claude `claude-haiku-4-5` 席

```bash
curl -sS -w '\nHTTP_STATUS:%{http_code}\n' -X POST https://app.devrelay.io/api/agent/raw-completion \
  -H "Authorization: Bearer <MACHINE_TOKEN>" -H "Content-Type: application/json" \
  -d '{"targetProjectId":"cmslf2jmd046mct0izdjr66qd","seatKey":"dangou-P05-haiku","ai":"claude","model":"claude-haiku-4-5",
       "system":"あなたは談合カードのプレイヤー P05 である。JSON のみで応答せよ",
       "prompt":"{\"question\":\"はい/いいえで答えよ: あなたはP05か\"}"}'
```

結果: `HTTP_STATUS:200`

```json
{
  "text": "```json\n{\n  \"answer\": \"はい\",\n  \"player_id\": \"P05\"\n}\n```",
  "output": "```json\n{\n  \"answer\": \"はい\",\n  \"player_id\": \"P05\"\n}\n```",
  "model": "claude-haiku-4-5-20251001",
  "usage": { "input": 410, "output": 281, "cacheRead": 0, "cacheWrite": 0 },
  "latencyMs": 8023,
  "agentDurationMs": 7999,
  "stopReason": "success",
  "sessionId": "raw_d47dd2df-12ee-4187-bd84-404deb72429c",
  "deniedTools": [],
  "ai": "claude"
}
```

`model` は Phase 1.4 `resolveRawUsedModel()` により実際に応答したモデル
（`claude-haiku-4-5-20251001`）を報告。`latencyMs=8023`。

参考: Claude 応答は JSON のみに厳密には従わず Markdown コードフェンス付きで返している
（system prompt の遵守度の問題であり raw-completion の実装とは無関係。ゲーム側で許容するか
system prompt を調整するかは別途検討）。

## デプロイ

`.ts` ファイルの変更は無い（テスト・README・doc のみ）。**`pnpm build` 実行済みだが、サーバーの
挙動に変わりはないため `pm2 restart` は不要**（稼働中サーバーは実機確認時点で既に Phase 2 dist を
読み込み済みで、今回のコード変更が無いことは上記 `git diff --stat` で確認済み）。DB マイグレーションなし。

## commit

- テスト2ファイル + README + changelog + devlog + INDEX
- `doc/devlog/2026-09-17_221048.md`（未コミットの前サイクル分、対象外）は本コミットに含めない
