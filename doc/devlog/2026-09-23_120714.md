# 2026-09-23 12:07 JST ｜ サイクル1.6: DevRelay Sites Phase 1-B B2-2 Rollback

## Requirements
B2-1 Stage A の 16 分計測完了を受け、dangou-card-viewer.devrelay.io の access log 設定を計測前の original 状態へ復元する（B2-3 着手前に必須の後片付け）。

## Execution
- dangou config（`/etc/caddy/sites.d/dangou-card-viewer.devrelay.io`）を backup `_etc_caddy_sites.d_dangou-card-viewer.devrelay.io.2026-09-22T22-15-36-434Z.bak` から original へ復元
- `/etc/caddy/sites.d/00-snippets` を削除
- Caddy reload 実施（Phase 1-B の reload 累計は B2-1 適用時の 1 回と合わせて **2 回**）

## Verification
- rollback 後、dangou の access log（`sites.access.log`）に新規行が **0 件**であることを確認（skip_hosts 側へ戻り、対象外ホストとして記録されないことを実測）
- dangou config に `import sites_access_log` / `log_skip` が含まれないこと（original と一致）を確認

## Findings
- 既存 rollback script（`sites-enable-access-log.ts` の `doRollback()`）が使う `sudoRemoveFile()` は `sudo rm -f <path>` を実行するが、sudoers NOPASSWD は `/bin/rm /etc/caddy/sites.d/*`（引数なしの 1 引数パターン）にしか一致しない。`-f` 付きは非対話で失敗しうる不整合があり、script 本体の修正は本サイクルでは未実施（B2-3 Plan の Findings にも再掲済み）。
