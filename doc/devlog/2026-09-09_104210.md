# 2026-09-09 10:42 JST ｜ サイクルC: Windows agent scope 受信配線

## 要求
Windows Agent 移植サブサイクル C（プラン `doc/plans/windows-agent-migration-plan.md`）。
`agentScopeId` / `resumeSessionId` / `turnId` の受信配線を `agents/windows/src/services/connection.ts` /
`ai-runner.ts` に追加し、MCP `approve_implementation` が Windows マシンで fail-closed のままになっている
問題（サーバー側 `planAiSessionId` が null のまま）を解消する。A(#378)/B(#379) で純モジュール配置・
ストア層置換済みだが呼び出し側が未配線。事前調査済みプラン: `/home/devrelay/.claude/plans/harmonic-wandering-tower.md`。
承認ノートでの追加指定: C1b は読み書き原子的に / F5 は E に固定 / commit 分割どおり進め時間切れは commit2 到達
優先 / `payload.resumeSessionId` はサーバー由来の既存 authenticated dispatch payload として扱う（新規外部
入力経路は作らない）/ commit2 は「MCP approve に必要なコード条件が揃う」までとし実通過確認は Windows 実機
E2E として本 devlog に手順を残す / テストは `--test-concurrency=1` で実数記録。

## 実行

### コミット 1: C1 + C2（スコープ配線 + resume 優先順位）— 完了
`connection.ts`:
- `SessionInfo` に `agentScopeId?: string` を追加（core#336 JSDoc 付き）
- `import { decideResume } from './resume-priority.js';` を追加
- `handleSessionStart` の payload 型 / 分割代入に `agentScopeId` を追加、`sessionInfoMap.set()` にセット（2 生成箇所のうち1つ目）
- `handleConversationExec` の payload 型に `agentScopeId?/resumeSessionId?/turnId?` を追加、else 分岐の
  `sessionInfo = { ... }` に `agentScopeId: payload.agentScopeId` をセット（2つ目）。既存 sessionInfo 側には
  Linux `:809-811` 相当の backfill（`if (payload.agentScopeId && !sessionInfo.agentScopeId) { ... }`）を追加
- `handleAiPrompt` の payload 型に `agentScopeId?/resumeSessionId?/turnId?/forceNewSession?` を追加
- exec → `handleAiPrompt(...)` 呼び出しに `agentScopeId: sessionInfo?.agentScopeId ?? payload.agentScopeId`,
  `resumeSessionId: payload.resumeSessionId`, `turnId: payload.turnId` を追加
- store 呼び出し 9 箇所に `agentScopeId`/`sessionInfo.agentScopeId` を配線（`loadConversation`×2,
  `loadClaudeSessionId`×1, `markExecPoint`×2, `saveConversation`×3, `clearClaudeSessionId`×1）
- `sendOptions` 構築の直前に `decideResume({ explicitResumeSessionId: payload.resumeSessionId,
  forceNewSession: payload.forceNewSession, storedSessionId: sessionInfo.claudeResumeSessionId })` を追加し、
  `resumeSessionId: effectiveResumeSessionId` に変更（**従来は `payload.resumeSessionId` を完全に無視していた
  欠陥を修正**）。`sendOptions` に `agentScopeId: sessionInfo.agentScopeId`, `turnId: payload.turnId`,
  `forceNewSession: payload.forceNewSession` を追加
- `:1232`（`handleAgreementApply`）は計画どおり変更せず（Linux も unscoped、`AgreementApplyPayload` に
  フィールドなし）

`ai-runner.ts`:
- `SendPromptOptions` に `agentScopeId?/turnId?/forceNewSession?`（JSDoc 付き）を追加
- `--resume` push に `if (options.resumeSessionId && !options.forceNewSession)` の二重防御ガードを追加
  （Linux の SDK 専用 `decideResume` 二重防御は移植せず、素のガードのみ）
- store 呼び出し 18 箇所（実測 19 呼び出し = `Promise.all` 内 4 件を含む）すべてに `options.agentScopeId` を配線:
  `loadDevinAtifStepOffset`, `loadDevinPermissionMode`, `loadDevinSessionId`, `loadDevinModel`,
  `clearDevinAtifStepOffset`（モデル変更時）, `loadCodexSessionId`, `saveCodexSessionId`,
  `saveClaudeSessionId`（**R-B2: 4引数形 `(projectPath, parsed.sessionId, undefined, options.agentScopeId)`
  で修正、3引数の誤りは無し**）, `saveContextUsage`, `saveDevinSessionId`, `saveDevinModel`,
  `saveDevinAtifStepOffset`, `saveDevinPermissionMode`, `clearDevinSessionId`/`clearDevinModel`/
  `clearDevinAtifStepOffset`/`clearDevinPermissionMode`（`Promise.all` 内）, `clearCodexSessionId`
- 非 claude（devin/codex）の stored ID resume は計画どおり `forceNewSession` で抑止しない（Linux と同挙動）

## 検証（コミット1）

1. `cd agents/windows && pnpm exec tsc --noEmit -p tsconfig.json` → エラーゼロ（出力なし）
2. `pnpm build`（リポジトリルート）→ 6 workspace すべて green（`packages/shared`, `agents/linux`,
   `agents/macos`, `agents/windows`, `apps/server`, `apps/web` すべて Done、web バンドル `index-DA0BSeTa.js`
   505.76 kB / gzip 138.57 kB）
3. `node --test --test-concurrency=1 tests/` を workspace ごとに個別実行（実数）:
   - `agents/windows`: **90 pass / 0 fail**（新規テストはまだ追加していないため既存 90 件がベースライン非退行）
   - `agents/linux`: **438 pass / 0 fail**（非退行）
   - `agents/macos`: **384 pass / 0 fail / 1 skip**（非退行）
4. grep 実測（配線の証拠。A/B では 0 件だった）:
   - `grep -c 'agentScopeId' agents/windows/src/services/connection.ts` → **22**
   - `grep -c 'turnId'` → connection.ts **5** / ai-runner.ts **1**
   - `grep -c 'forceNewSession'` → connection.ts **4** / ai-runner.ts **3**
   - `grep -n 'saveClaudeSessionId(projectPath, parsed.sessionId)' agents/windows/src/services/ai-runner.ts`
     → **0 件**（R-B2 の 2/3引数形が残っていないことを確認）
5. `git diff --stat` → `agents/windows/src/services/ai-runner.ts` (50行) と `connection.ts` (57行) の
   2 ファイルのみ。`apps/`・`packages/`・`agents/linux`・`agents/macos` は無変更
6. `git diff` に `.devrelay/sessions` へのハードコードパスなし（すべて `resolveScopeDir` 経由の既存ストア
   関数呼び出しに `agentScopeId` 引数を追加しただけ）

コミット1完了、git commit 実施予定。続けてコミット2（C3 エコーバック）に着手する。

## 発見
（コミット1では設計変更なし。プラン文書どおりに実装完了。以降のコミットで発見があれば追記）
