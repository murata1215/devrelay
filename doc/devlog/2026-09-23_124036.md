# 2026-09-23 12:40 JST ｜ サイクル1.7: DevRelay Sites Phase 1-B B2-3 Stage B

## Requirements
B2-3 Stage B（dangou-card-viewer.devrelay.io への `vars.log_skip:true` によるポーリングログ除外を適用した状態での 16 分計測）の**最終化のみ**を行う。計測自体（T0=12:12:01 JST 〜 T1=12:28:01 JST）は前 submission で開始済みで、T1 は着手時点で既に経過していたため、本サイクルは待機・再計測を一切行わず、`/tmp` の既存証跡と access log を読むだけで完結させた。

## Execution
1. **完走証跡確認**（read-only）: T0/T1 marker（`epoch=1790133121`/`1790134081` = 12:12:01/12:28:01 JST）、`load script finished`、`polling done: count=463 ok=463 fail=0`、PID 消滅、stdout/stderr 0 バイトをすべて確認。証跡は完全に揃っており再計測不要と判断。
2. **Stage B raw 集計**（`/tmp/b2_3_stage_b_rawstats.py`、`/tmp/b2_1_stage_a_rawstats.py` ベース・host固定・polling regex 2種で算出、リポジトリ外）
3. **独立 aggregator**（`/tmp/b2_3_stage_b_agg.mjs`、`apps/server/dist/services/sites/*` の純粋関数のみ import、DB/サーバー本体 state 非接触、UU secret は read-only 直読み・値非出力、リポジトリ外）
4. **Stage A 比較**（下表）
5. **health 差分**（read-only、baseline と完全一致、新規悪化なし）
6. 本 devlog 新規作成 + INDEX.md 末尾 1 行追記

## Stage A vs Stage B 比較

| 指標 | Stage A（フィルタ前） | Stage B（`log_skip` 適用後） |
|---|---|---|
| 計測窓 | 16分 | 16分 |
| server 送信総数 | 523（実測=access log lines） | 485（polling463 + 非polling22、送信側 progress 由来） |
| access log lines | 523 | **54** |
| bytes（access log 1行のバイト長合計） | 291,657 | **35,207**（削減率 **87.93%**） |
| avg bytes/line | 557.66 | 651.98（残存行が非pollingのみに偏るため上昇） |
| polling 送信 count/ok/fail | 469（ログ残存、想定480比-11） | 送信463 / ok463 / fail0（送信側） |
| polling access-log 残存件数 | 469 | **0**（`state$` regex・`state/?$` regex とも一致） |
| polling log削減率 | — | **100%**（463件中0件残存） |
| polling 件数比率 | 89.67% | — |
| polling bytes比率 | 87.93% | — |
| PV | 18 | **18**（維持） |
| UU | 1 | **1**（維持） |
| Referer | direct15/zenn.dev3 | direct15/zenn.dev3（**維持**） |
| UTM campaign | `b2_1_stage_a_resume`×3 | `b2_3_stage_b_resume`×3（campaign名以外は同一構造） |
| 4xx / 5xx | 18 / 0 | **18 / 0**（維持） |
| bot 送信数（`/`宛） | 2 | 2（残存2、bot はポーリング対象外のため非削減が正） |
| `excludedByRules` | true | true |
| 1日換算 bytes（×90） | 26,249,130 | **3,168,630** |
| 30日換算 bytes（×2700） | 787,473,900 | **95,058,900** |

**bytes削減率87.93%はStage Aの`polling_ratio_bytes`（87.93%）と完全一致** — pollingログ行を丸ごと除外すれば理論値と一致するはずで、2系統の独立集計（raw集計・独立aggregator）が整合していることの根拠になる。

**除外対象外API（commentary）の残存確認**: 窓内の `other /api/*` は Stage A/B とも `/api/games/*/*/commentary` 16件で完全一致。`log_skip` ルール（`^/api/games/[^/]+/[^/]+/state/?$`）が polling パスのみを狙い撃ちし、他の API パスには影響しないことを実測で確認した。

**非polling内訳（`/`=32, `/watch`=4, `other /api/*`=16, `other`=2、計54行）はStage AとStage Bで完全一致** — 両ステージの計測条件が同一であることの裏付け（合成負荷スクリプトの非pollingリクエスト数は22件だが、既存のバックグラウンドトラフィックが両ステージで同数観測されており、両者は同一環境下の比較として成立している）。

## Verification
- 独立2系統（`rawstats.py`・`agg.mjs`）で access log lines=54 が一致
- polling残存0件 + 非polling54件 = access log lines 54件が成立
- Stage Aの非polling行数54とStage Bの行数構成（54）が一致
- `dangou-card-viewer` health は計測前後とも 200（機能退行なし、`apkserver`/`game001`/`hanabitest`/`test004`は既知502、`chrome-bookmark`は既知404のまま新規悪化なし）
- `git status --short` で `/opt/devrelay` の差分は本 devlog + `INDEX.md` 1行のみ
- Caddy reload: **今回0回 / B2-3全体1回（T0前に適用済み）/ Phase 1-B累計3回**（B2-1適用1・B2-2 rollback1・B2-3適用1）
- pm2 restart なし・DB操作なし・commit/push なし・`.ts` 変更なし
- B2-4 / W2 は未着手

## Findings
- Caddy raw JSON の `size` フィールド（レスポンス body バイト数）と Stage A の `total_bytes`（access log 1行のバイト長）は**別指標**。混同すると桁が合わない（`size`合計は約60万バイトだが`total_bytes`は29万バイト台）。Stage B もStage A互換の `line_len` 方式で算出し、次サイクル以降の誤読を防ぐため本注記を残す。
