# 2026-09-26 14:01 JST ｜ サイクル: DevRelay MCP に ask_project / get_answer / cancel_submission を追加

要求: test010 のチャッピー（GPT-Live + DevRelay MCP、PAT 認証）から各プロジェクトの AI エージェントに
質問したいが、既存 MCP 8 ツールには質問専用ツールが無かった。回避策（ask-member の代用、
submit_instruction をプランで止める）はどちらもマシントークン認証・同期ブロッキング・未承認
submission の永久残留という問題を抱えていたため、専用ツールを新設する調査・実装依頼だった。
調査サイクルで submission=`Session`行のモデル・AI Message は完了時のみ作成される構造・
strictReadonly の実効性が AI ツールにより異なる（claude/codex は権限で強制、devin/gemini/
terminalMode は強制不可）ことを確認し、プラン承認後、人間判断（AI 差し替えなし、exec中cancelは拒否）
を反映して実装した。

実行: `apps/server/prisma/schema.prisma` に `Session.kind`/`Session.cancelledAt`（nullable、
バックフィルなし）を追加、`npx prisma db execute --stdin` で直接適用（`migrate dev` はシャドウDBの
既存ドリフトで失敗、既存の手適用と同じ運用に合わせた）。新規 `ask-guard.ts`（外部import ゼロの
純関数群、`decideAskReadOnlyEnforcement`/`buildAskPromptPrefix`/`deriveAskState`/`decideCancel`等）
を作成し、`mcp/tools.ts` に `ask_project`/`get_answer`/`cancel_submission` の3ツールを追加。
`submission-guard.ts` の `evaluateApproveGuard` に `cancelled`/`questionNotApprovable` の2判定
コードを追加（既存5コードの判定順・文言は不変）。approve/cancel の claim where にそれぞれ
`cancelledAt: null`/`approvedAt: null` を条件化し、atomic な相互排他を成立させた。`GET /api/threads`
に `kind`/`cancelled` を追加し、WebUI（ThreadList.tsx・thread-list-rules.ts・messages.ts）に
「質問」「取消済み」バッジを追加。Agent側（agents/linux, agents/macos, packages/shared）は
一切変更していない（既存 permissionPolicy='ask' の wire フィールドのみ使用）。

検証: `npx prisma db execute --stdin` でカラム存在をSQLで確認（kind: text nullable, cancelledAt:
timestamp nullable）、既存行の kind IS NOT NULL 件数が0であることを確認（バックフィルなし）。
`pnpm build` で shared/agents(linux/macos/windows)/server/web の6ワークスペース全てDone、
web dist の `grep -c 'require('` が0であることを確認（#309→#310白画面事故の再発防止チェック）。
`node --test tests/` で server 736件green（新規 ask-guard.test.mjs 39件・mcp-ask-source-guard.test.mjs
11件、submission-guard.test.mjs +4件、permission-policy.test.mjs コメント更新のみ）、web 518件green
（新規 isQuestionThread/isCancelledThread 6件）。commit/push・pm2 restart・実機E2Eは未実施
（本番サーバーのため、ユーザーのテスト全件確認後の指示を待って実施する）。

発見: 本番DB実測で defaultAi/terminalMode の組み合わせは claude非terminal 146件・devin 30件・
claude+terminalMode 13件・codex 1件（gemini 0件）。既存 `get_build_status` ツールがuserId所有
チェックを欠いている（他8ツール中唯一）ことを調査で発見したが、今回のスコープ外として未修正
（rules/project.md に既存の設計判断として記録済み）。migrations/ ディレクトリは PersonalAccessToken
等の既存カラム同様、手適用のため migration ファイルを作成しない運用を踏襲した。
