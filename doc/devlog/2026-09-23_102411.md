# 2026-09-23 10:24 JST ｜ サイクル1.5: DevRelay Sites Phase 1-B B2-1 Stage A

## Requirements
前 submission（`cmuddtlhp00zml74bszgxqq20`）で 16 分の制御合成負荷計測窓（T0=09:56:28 JST〜T1=10:12:28 JST、対象 `dangou-card-viewer.devrelay.io`）は正常完走したが、T1 直後にツール権限が切れ Step 5（終了マーカー）以降の最終集計・devlog 作成が未完了だった。本サイクルは新しい計測窓を開始せず、既に閉じた T0〜T1 の raw log のみを集計して Stage A を完了させる。Caddy 設定変更・reload・rollback は一切行わない。

## Execution
1. ツール権限復帰を read-only で確認（access log 読み取り可、`/tmp/b2_1_stage_a_*` 作業ファイル7件健在、pm2 pid=1679601/restarts=15 不変、fd→access log 保持）。
2. synthetic load process（pid 1711375）は既に自然終了済み（`load.progress`: `finished at 2026-09-23T01:12:53Z`）であることを確認、停止操作は不要だった。
3. Step 5: T1=1790125948 を終了マーカーとして固定、参考値を `/tmp/b2_1_stage_a_marker_T1.txt` に記録（inode 一致、pm2 情報不変を確認）。
4. Step 6: 使い捨て `/tmp/b2_1_stage_a_rawstats.py` に requests/log lines 分離ロジックと想定 polling 件数(480)との差分出力を追加し、T0<=ts<T1 で raw log を再集計。
5. Step 7: 既存 `/tmp/b2_1_stage_a_agg.mjs`（無修正）で dist の純粋関数のみを使った独立集計を実行。UU secret は `getOrCreateUuSecret()` を呼ばず read-only 直読み（rev.8 方針維持、値は非出力）。
6. baseline health は read-only で全サイトを再確認（`dangou-card-viewer` は 200 OK、`game001` は既知の 502 のまま）。

## Verification
Stage A 結果（T0〜T1、16分）:

| 項目 | 値 |
|---|---:|
| requests | 523 |
| log lines | 523 |
| requests/min | 32.69 |
| lines/min | 32.69 |
| bytes | 291,657 |
| bytes/min | 18,228.6 |
| avg bytes/line | 557.7 |
| polling件数 | 469（想定480との差 -11） |
| polling成功件数 | 469（成功率100%） |
| polling比率(requests) | 89.7% |
| polling bytes比率 | 87.9% |
| 1日換算 requests | 47,070 |
| 1日換算 bytes | 約25.0MB |
| 30日換算 requests | 1,412,100 |
| 30日換算 bytes | 約751.1MB |

独立 aggregator（rules 期待値との照合、すべて一致）: `/` `/watch` 200→PV（PV合計18=`/`14+`/watch`4）、state polling→PV 0、404(18件)→4xx+1・PV 0、bot(Googlebot UA)→bot 計上・PV 0、`DevRelay-Sites/1.0` UA→集計除外(`excludedByRules:true`)、UTM付き`/`→PV+UTM(source=zenn/medium=article/campaign=b2_1_stage_a_resume 各3件)、Referer→hostname分類(zenn.dev 3件/direct 15件)、UU=1（同一送信元のため妥当）、measuredSince=2026-09-23・coverage.timeZone=Asia/Tokyo。raw IP は一切出力していない。

Caddy: reload累計1回のまま・今回追加reload 0・config変更0、`log1`(`http.log.access.log1`)→`/var/log/caddy/sites/sites.access.log`の対応関係を維持確認。self-heal: 本サイクルは access-log-reader の再オープン発火なし（fd継続保持のため対象外）。log owner/group/mode: `caddy:devrelay 0640`。pm2: pid/restarts不変。backup: `~/.devrelay/sites-backups/_etc_caddy_sites.d_dangou-card-viewer.devrelay.io.2026-09-22T22-15-36-434Z.bak` 健在。

## Findings
- T1 直後のツール権限切れにより、Step 5 以降（終了マーカー・最終集計・devlog作成）は前 submission とは別 submission として本サイクルで完了させた。計測窓自体のやり直しは行っていない。
- 09:37 の site health baseline スナップショットは `/tmp` に永続化ファイルが見つからず正確な差分比較はできなかったが、read-only 再確認で計測対象の `dangou-card-viewer` は200 OK、`game001` は既知の502のまま新規悪化なしと判断した。
- 保持設定（roll_size/roll_keep）は本サイクルでは決定していない。B2-2以降は別承認が必要。
