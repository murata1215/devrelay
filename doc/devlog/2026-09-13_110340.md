# 2026-09-13 11:03 JST ｜ サイクルP1.2: Capability 配布 — items 空の reconcile / prelaunch を items に依存させない

要求: P1.1 devlog の申し送りどおり、`capabilityConfig.items` が空でも `providers.claude`（marketplace）が設定されていれば machine reconcile（marketplace add/update）が走り、prelaunch も `.claude/settings.json` の `enabledPlugins` 宣言を Machine の items 件数に依存せず処理するよう修正した。スコープは `agents/linux` + `apps/web` の hint 文言 1 行のみ、server/shared/prisma/agents/macos/agents/windows は無変更。

実行: `capability-rules.ts` に `listConfiguredProviders` / `resolveReconcileTargets`（items 先行 + 未登録 provider のみ registry から追加、回帰テストで順序固定）/ `hasReportableOutcome` を追加し、`capability-sync.ts` の `runMachineReconcile()` と `reconcileForRunner()` をこれらの純関数駆動に置換。prelaunch は「providers.<provider> 未設定」を TTL キャッシュを書く前に判定して return するよう順序を修正（先にキャッシュを書くと設定直後の5分間 prelaunch が死ぬため）。prelaunch の時間予算を承認ノートの指摘どおり 5 秒→3 分（`ADAPTER_TIMEOUT_MS`、§7.3 準拠）に変更、差分が無ければ CLI を呼ばず即起動する既存の early-return 原則は維持。`claude-plugin-rules.ts` に `resolveFailureIds` を追加し、`claude-plugin-adapter.ts` の `missing-provider-config` / `claude-not-found` / `marketplace-*` failed 経路が items 0 件でも無言成功にならないよう fallback ID を適用（`marketplace-update-failed` push の後に items 0 件の早期 return を配置し、順序を誤るとオフライン機の失敗が消えることに注意）。web は `MachinesPage.tsx` の emptyTargets ヒント文言を「Plugin 未指定のため marketplace 登録も行われません」から「有効な配布設定（Marketplace）が見つかりません」に更新（`capability-config-rules.ts` はロジック無変更、JSDoc のみ追記）。

検証: `pnpm build` 6 workspace green。`node --test` を workspace ごとに個別実行し agents/linux 540→575 pass（新規35件、items 2件保持の回帰テスト含む）、apps/web 441/441・apps/server 424/424・packages/shared 47/47 すべて非退行。`grep -c 'require('` on apps/web/dist = 0。`git diff --stat -- apps/server/ packages/ prisma/ agents/macos/ agents/windows/` は空を確認しスコープ逸脱なし。変更ファイルは agents/linux 7本（新規1本含む）+ apps/web 3本の計10本のみ。

発見・申し送り: §8.2 の初回フォールバック（prelaunch 中の marketplace add）は P1.3 送りとした。理由は `withTimeout()` が timeout 時に `execFile` をキャンセルしないため、`marketplace add`（git clone、3分予算に対し実測でも数十秒〜掛かりうる重い処理）を prelaunch の限られた予算で走らせると、結果が捨てられた後も `~/.claude/` を書き換えるプロセスが孤児として残り、既に起動した Claude Code と競合する危険がある（現状の「何もしない」より悪化）。代替設計案: prelaunch は `known_marketplaces.json` を read するだけで未登録を検知し、`requestReconcile('config')` を積んで即 return、重い処理は 3 分予算の machine キューに任せる。P1.2 完了時点で P1 全体はまだ完了ではなく、残件は P1.3（初回フォールバック）/ E2E-A（prelaunch の実機検証）/ E2E-C（updated 判定の実機検証）。
