# 2026-09-23 14:45 JST ｜ サイクル2.0: DevRelay Sites Phase 1-B W2 Rollout

## Requirements

pre-W2 safety fixes（サイクル1.9、A〜G実装・671/671 green）と B2-4 最終設定値確定を踏まえ、
固定 21 host へ access log 設定を本番 rollout する。対象は下記 W2_TARGET_HOSTS（`site-log-rules.ts`）。
W3（manual 3 host / Caddyfile inline 8 host）・polling regex・retention 値・fixed21 の変更は対象外。
Plan: `/home/devrelay/.claude/plans/mossy-sparking-sphinx.md`

固定21 host: apkserver, chrome-bookmark, freecell, game001, game002, game2048, hanabitest,
klotski, manager, mimamori-server, nim, pixql, test004〜test010, tetris, vixbtc（全て `.devrelay.io`）。

## Execution

1. **preflight**: Caddy active/MainPID 790/NRestarts 0、pm2 devrelay-server pid 1750531/restarts 16、
   sites.d baseline md5 `537d3c86245cad9235d3a594933dab2a`、Caddyfile md5 `50ee6b26df03b5d9c3c9fda37dbc20cc`、
   log dir `caddy:devrelay 2750`、UU secret `devrelay 600`（値は出力せず所有者/mode のみ確認）、
   W3 固定集合（sites.d 3件 + Caddyfile inline 8件）不変、`ROLLOUT_ROLL_CONFIG` = `64MiB/12/1440h/provisional:false`
   を再確認。health baseline を 22 host（21 + dangou）に対し 1 host 1 リクエストで取得
   （`DevRelay-Sites-ReadOnlyCheck/1.0`）。game001 の既知 502 に加え、apkserver/chrome-bookmark/
   hanabitest/test004/005/006/007/009/010 も既存の 502/404（Caddy設定変更と無関係なバックエンド未起動）
   と判明、これらを baseline として記録し「継続は FAIL にしない」対象に含めた。
2. **final dry-run / verify-mirror**: `--dry-run` で対象21 host が `W2_TARGET_HOSTS` と完全一致・
   dangou は already-applied で除外を確認。`--verify-mirror` PASS（logger 22/health matcher 22/
   roll設定1種/複数logger host 0）。純粋関数（`applyLogConfig`/`renderSnippet`）を用いた自前ミラーで
   semantic diff を取得し、許容差分が「00-snippets の retention+health skip」と
   「21 host への import 1行追加」の2種類のみであることを確認。
3. **`--no-reload` フラグ追加**（`apps/server/scripts/sites-enable-access-log.ts` のみ、約15行）:
   apply 後の自動 reload を止め、reload 前に本番実ファイルへ semantic diff/adapt 構造検証を
   挟めるようにした（現行スクリプトは apply→adapt→reload が1プロセスで連続実行される設計だったため、
   Plan の Step 4 要件を満たすために必要な最小変更）。`src/services/sites/*` の純粋ロジックは無変更。
   `node --test tests/` で 671/671 green 維持を確認、`--dry-run --no-reload` の出力が
   既存 `--dry-run` と完全一致することを確認。
4. **batch backup**: 実行前 `--list-backups` で有効batch 0件を確認。
5. **W2 apply**: `npx tsx scripts/sites-enable-access-log.ts --apply --no-reload` を実行。
   batchId `20260923T053916Z-f3169e99` 発行、manifest 22 entries（`00-snippets` + 21 host、
   全て `action:modified`）、backup file `b0001.bak`〜`b0022.bak` 生成、`caddy adapt` 成功、
   reload はスキップ（本番 Caddy はこの時点でも旧設定のまま稼働継続）。
6. **pre-reload validation**: 変更ファイルは想定どおり22件のみ（dangou・W3 3件・Caddyfile 本体は
   md5 不変）、21 host 全て `import sites_access_log` ちょうど1回・inline 0、重複 matcher 定義なし
   （定義行 `@drl_skip_* {` のみで判定、参照行 `log_skip @name` は別カウント）。本番 Caddyfile に対する
   `caddy adapt` 実行結果へ、実装と同一の `verifyAdaptedConfig` 純粋関数をそのまま適用し
   `ok:true`（logger 22/health matcher 22/roll設定1種/複数logger host 0）を確認。
7. **Caddy reload**: 全条件PASSを確認後 `sudo systemctl reload caddy` を1回だけ実行（exit 0）。
   **Phase 1-B 累計 4 回目**（今回 1 回）。reload 後も MainPID 790・NRestarts 0 不変、
   pm2 devrelay-server pid 1750531・restarts 16 不変、log dir/file の owner・mode 不変を確認。
8. **実リクエスト検証**（reload後の access log 追記分のみを対象、1 host あたり 1 リクエスト）:
   a) tetris への通常UAリクエスト → 追記1行（記録される）。
   b) tetris への `DevRelay-Sites/1.0` リクエスト → 追記0行（health除外が機能）。
   c) tetris への `DevRelay-Sites-ReadOnlyCheck/1.0` リクエスト → 追記1行（前方一致に巻き込まれず記録）。
   d) dangou への polling パス（`/api/games/zzz/zzz/state`） → 追記0行（polling skip 維持）。
   e) dangou への非pollingパス（`/api/commentary`） → 追記1行（非polling APIは記録される）。
   f) health-checker の自動周期（60秒間隔、既定ON・env未設定で確認）を2周期分（130秒）待機し、
      追記分に `DevRelay-Sites/1.0` の出現が0件であることを確認（b の直接検証と整合）。
9. **health baseline 差分**: reload後に Step preflight と同一手順で22 host を再取得し diff → **完全一致
   （差分0）**。game001の502含む既知障害は全て継続のみで新規悪化なし。
10. **rollback readiness**: 最終 `--rollback 20260923T053916Z-f3169e99`（dry-run preview）で
    restore 22件・drift 0件を再確認。

## Verification

- `node --test tests/`: 671/671 green（`--no-reload` 追加後も維持）
- `--verify-mirror`: 2回実行いずれもPASS（フラグ追加前後）
- 本番 `caddy adapt` + `verifyAdaptedConfig`: reload前・reload後の両方で `ok:true`
  （logger 22/health matcher 22/roll設定1種/複数logger host 0/orphan logger 0）
- md5 diff: 変更ファイルは `00-snippets` + 固定21 host の計22件のみ。dangou・W3 3件・
  Caddyfile 本体（md5 `50ee6b26df03b5d9c3c9fda37dbc20cc`）は不変
- 実リクエスト a〜e: 期待どおりの行数（1/0/1/0/1）で全PASS
- health baseline: reload前後で22 host 全て同一ステータスコード（差分0）

## Findings

- fixed21: apkserver/chrome-bookmark/freecell/game001/game002/game2048/hanabitest/klotski/
  manager/mimamori-server/nim/pixql/test004〜test010/tetris/vixbtc（`.devrelay.io`、全21件）
- batchId: `20260923T053916Z-f3169e99`（manifest 22 entries、backup先
  `~/.devrelay/sites-backups/20260923T053916Z-f3169e99/`）
- reload: **今回1回・Phase 1-B累計4回目**
- logger数: 22（21 + dangou）、1 host = 1 logger、複数logger host 0、orphan/dangling logger 0
- health exclusion: UA完全一致（`DevRelay-Sites/1.0`）、matcher出現数22（=logger数と一致）、
  実測でReadOnlyCheck UA等の前方一致巻き込みなしを確認
- retention: `64MiB/12/1440h`（`00-snippets` 経由で dangou も同時に pilot値
  `32MiB/8/168h`→rollout値へ変更・health skip新規有効化。仕様どおりで異常ではない）
- game001の既知502: rollout実行後も継続のみ、新規悪化なし。他host（apkserver/chrome-bookmark/
  hanabitest/test004/005/006/007/009/010）の既存502/404も同様に継続のみ（Caddy設定変更とは
  無関係なバックエンド未起動が原因、rollout前後で完全一致）
- rollback readiness: exact batchId一致でのみ実行可能、preview で restore 22件・drift 0件を確認済み
- コード変更: `apps/server/scripts/sites-enable-access-log.ts` に `--no-reload` フラグを追加
  （既定off、shell層のみ、`src/services/sites/*` 純粋ロジックは無変更）
- **W3（manual host・Caddyfile inline host）は未実施**（対象外のまま）
- **commit/push は未実施**（作業ツリーに `--no-reload` 追加分が残存）
- **`pnpm build` 不要・pm2 restart 不要**（`scripts/` は `npx tsx` 直実行、`src/` 非変更のため
  devrelay-server の稼働プロセスに影響なし）
- DB操作なし・新規合成負荷なし・polling regex/retention値/fixed21の変更なし
