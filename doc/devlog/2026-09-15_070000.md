# 2026-09-15 07:00 JST ｜ サイクルP3-A: Devin native skill 配布 adapter（`devin:skill`）実装

要求: P1〜P1.3 で作った Capability 配布基盤は provider×kind をキーとする adapter レジストリを持ちながら実装済み adapter が `claude:plugin` 1 つだけで、抽象が本当に機能するか未実証だった。実機（DESKTOP-1E6SDOQ/MURATA_K）で Claude プラグインの `skills/<name>/` を Devin CLI のグローバル skills dir にそのままコピーすると SKILL.md を書き換えずに Devin の `available_skills` に載ることを確認済みで、本サイクルは 2 番目の adapter `devin:skill` を実装し provider×kind 抽象を実証した。プラン `/home/devrelay/.claude/plans/floofy-giggling-pine.md`、承認ノート `doc/p3a_devin_skill_adapter_approval_note.md`（11件の確定判断で本文と食い違う箇所を上書き）。

実行: Step 0 の read-only 調査で指示書との差分3件を確定（`packages/shared` の型変更が必須／`providers.devin.skillsDir` はサーバー検証器が未知キーを黙って捨てるため通せない／server は完全に無変更で済む）。**D1**（純関数のみ・呼び出し元ゼロ）: `devin-skill-rules.ts`（パス解決・URL変換・manifest parse・desired plan構築・marker検証・action/removal判定・ID整形、外部importゼロ）+ `devin-locator.ts`。**D2**（I/O部品）: `git-cli.ts`（`execFile`固定・非対話env強制の`GitCliRunner`）+ `skill-tree-io.ts`（symlink安全な再帰コピー/sha256/containment検査/staging→trash→dest のアトミックswap/`removeDirTree`）+ `devin-path.ts`。**D3**（adapter本体）: `devin-skill-adapter.ts`（deps注入形、`reconcileMachineWithDeps`/`reconcileProjectWithDeps`）を新設し `connection.ts` に `registerCapabilityAdapter(devinSkillAdapter)` を1行追加、テスト作成中に発見した「clone失敗時に部分clone残骸のcleanup呼び出しをskill削除とみなしてしまう」assertion不備を修正（`dirname(dir)===skillsDir`でスコープを絞り、CRITICAL RULE=索引取得失敗時はskillを一切消さないことをより正確に固定）。**D4**（撤去経路）: `capability-rules.ts` に純関数 `resolveCleanupKeys(registryKeys, coveredKeys, managedKeys)` を追加（`resolveReconcileTargets`自体は無変更のまま）。`capability-sync.ts` に `CapabilityAdapter.hasManagedState?()`（optional、未実装adapterは撤去経路に構造的に入らない）、`configDelivered`フラグ（`capabilityConfig`キー自体がpayloadに一度でも存在したか。null値でもtrue。P1未満サーバーへのロールバック事故防止）、`EMPTY_CONFIG`、`runCleanupPass()`を追加し `runMachineReconcile()` に配線（machine経路のみ、prelaunchは無変更）。`MergeableResult`/`CapabilityResult`相当に`removed?: string[]`を純加算（非空時のみキーが生える）。**D5**: `packages/shared/src/types.ts`の型追加（`CapabilityDevinProviderConfig`/`providers.devin?`/`CapabilityResult.removed?`）はセッション開始前に別サブサイクルで完了済みと確認。本サイクルではweb側 `capability-config-rules.ts`（`CapabilityConfigFormState.distributeToDevin`、`capabilityConfigToFormState`/`validateCapabilityForm`の往復変換でclaude→devin順を固定、`decideSyncStatusDisplay`に`results.length>1`のときだけ生える`perProvider`内訳）+ `MachinesPage.tsx`（「Devin にも配布する」チェックボックス1個 + state配線 + per-provider表示行）を実装。

検証: `pnpm build` 6 workspace green（apps/server含め全て）、`grep -c 'require('` on `apps/web/dist/assets/index-*.js` = 0。テストは workspace ごと個別実行（#333の教訓）: `packages/shared` 47/47（型のみ、変更なし）・`apps/server` 443/443（無変更、非退行）・`agents/linux` 759/759（新規: devin-skill-rules 61・devin-locator 8・skill-tree-io 15・devin-skill-adapter 23・capability-rules 67（+11）・capability-sync-targets 21（+8）、既存 claude-plugin-adapter 18/claude-plugin-rules 61 は非退行）・`apps/web` 451/451（capability-config-rules 37、+10。既存の`capabilityConfigToFormState: null は空フォーム`のdeepEqualに`distributeToDevin: false`を追加する編集込み）。`git diff --stat -- apps/server/ prisma/ agents/macos/ agents/windows/` 空でスコープ逸脱なし。撤去経路（cleanup パス）は fake adapter でのケース(b)（providers から外れた managed devin の撤去）・ケース(c)（`capabilityConfig`全体null時の`configDelivered`ゲート）・`hasManagedState`未実装のClaude fakeが呼ばれないこと・throw時のfail-closedをすべてテストで固定。

発見・申し送り: 仕様書 `doc/devrelay_capability_spec_v2.md` を v2.2 に改訂（adapter契約への`hasManagedState?()`追加・`CapabilityResult.removed?`型解禁の理由・§8.3新設・§7.3予算内訳・provider固有フィールドがサーバーを通せない制約を明記）。**ロールアウト順序の制約（重要）**: D1〜D4（Agent側）を先に commit + push + 各機`u`してから、D5のチェックボックスを使うこと。逆順だと devin adapter を持たない旧 Agent に `devin:skill` items が届き `unsupported-provider` で claude成功時もマシン全体が`status:'error'`表示になる。**E2E（実機、DESKTOP-1E6SDOQ/MURATA_K）は本サイクルでは未実施**——プランの§7に手順を記載済み。`updated`判定の実機検証は索引リポ・プラグインの中身を変更しない制約のため自動テストのfixtureのみで確認、実リポのversion bumpを使ったE2Eは次サイクル送り。**人間側**: `pnpm build`実行済み・**`pm2 restart devrelay-server`は不要**（`apps/server`無変更、webはCaddyがディスクから直接配信のためbuildのみで反映）・DBマイグレーション不要（`capabilityConfig`/`capabilitySyncStatus`は既に`Json?`）。**commit + push が必須**、その後 DESKTOP-1E6SDOQ/MURATA_K（E2E機）および devin を使う他機で `u`（`/opt/devrelay`の変更はpushしないとAgentに届かない）。
