# 「(無題)」大量発生の根治 サイクルA〜C — commit + Step 0 SQL 実測

## 背景

前サイクル（サイクルA〜C）で以下を実装済みだったが、実行承認メッセージに commit 指示が
含まれていなかったため、作業ツリーに未 commit のまま残っていた。

- サイクルA: `GET /api/threads` で ended かつ Message 0 件の抜け殻スレッドを一覧から隠す
  （キルスイッチ `DEVRELAY_THREADS_HIDE_EMPTY_ENDED`、既定 `1`）
- サイクルB: タイトル導出の強化（N+1 解消 + コマンドタグ/AI ノイズ除去ラベル）
- サイクルC: `//connect` の ended 復活を「Agent がオフラインの場合のみ」に緩和

本サイクルでは、右レール移設タスクに着手する前の Step 0 として、この未 commit 分を
別 commit として確定し、前サイクルで未実行だった「分類 1〜4 の件数」SELECT を実際に
実行して数値を記録する。

## Step 0 SQL 実測結果

対象: `Session.status IN ('active','ended')` かつ `title IS NULL OR btrim(title) = ''`
（`teamexec_%` / `crossquery_%` / `askdesc_%` の内部セッションは除外、削除済み Project/Machine も除外）。

### B-0: 分類 1〜4 の件数（直近 7 日 / 全期間）

| bucket | status | last7d | all_time |
|---|---|---|---|
| 1: Message 0件 | active | 5 | 5 |
| 1: Message 0件 | ended | 102 | 696 |
| 2: userメッセージ0件（AI応答のみ） | ended | 0 | 3 |
| 3: 先頭userがコマンド | ended | 8 | 205 |
| 4: それ以外 | active | 3 | 4 |
| 4: それ以外 | ended | 98 | 1706 |

**読み方**: 「無題」の主因は bucket 1（Message が 1 件も無い抜け殻セッション、全期間 701 件）と
bucket 4（メッセージはあるが title 導出に失敗しているもの、全期間 1710 件）の 2 つで大半を占める。
bucket 3（先頭 user メッセージがコマンドタグ）は元々の仮説だったが全期間 205 件と相対的に少数
（先行の `refactored-stargazing-hamster.md` A-4 での「コマンド仮説は不成立」という調査結論と整合）。

### B-1: プロジェクト別件数（上位、全期間の「無題」件数降順）

| project | 件数 |
|---|---|
| Lafit | 161 |
| test | 77 |
| immail | 56 |
| aisignage | 55 |
| pixblog | 44 |
| Workshop | 26 |
| FORKCAST_PoC | 26 |
| Lafit (workspace) | 21 |
| agent | 19 |
| pixdraft | 17 |
| devrelay_flutter | 15 |
| devrelay | 14 |
| osrm | 13 |
| signage-server | 13 |
| clipped | 12 |
| pixshelf | 9 |
| pixnews | 9 |
| pixblog-flutter | 9 |
| sharepoint_download_folder | 8 |
| devrelay-manager | 8 |

### B-2: 修正後の日次件数（デプロイ前ベースライン）

本サイクルの commit はまだ `pm2 restart` されていないため、修正コードは未デプロイ。
実測時点（2026-09-12）でも抜け殻セッションの発生が継続していることを確認（当日分のみ抜粋、
`active` 状態で aisignage / devrelay-manager / test007 の 3 件を含む）。
これは**修正コードが機能していないことの証跡ではなく**、単に「まだ動いていないコードの
実測前ベースライン」である点に注意。サイクルA〜Cの効果検証（B-2 の継続観測）は
`pm2 restart` 後に改めて行う必要がある。

## commit

前サイクルの実装（apps/server + apps/web、13 ファイル、+560/-30）を、本サイクルとは別の
単独 commit として確定した。

- commit: `69f9ff2`
- 対象ファイル: `apps/server/src/routes/api.ts`, `apps/server/src/services/command-handler.ts`,
  `apps/server/src/services/thread-reestablish.ts`, `apps/server/src/services/thread-label-source.ts`（新規）,
  `apps/server/src/services/thread-list-filter.ts`（新規）, 各テストファイル,
  `apps/web/src/components/ThreadList.tsx`, `apps/web/src/lib/api.ts`, `apps/web/src/lib/thread-list-rules.ts`,
  そのテストファイル, `CLAUDE.md`（`DEVRELAY_THREADS_HIDE_EMPTY_ENDED` の env 表追記）
- `git add` はパス指定のみ（`-A` 未使用）

## 検証結果

- `apps/server`: pretest（tsc）green、`node --test` → 406/406 pass
- `apps/web`: pretest（tsc）green、`node --test` → 327/327 pass

## 反映

- `apps/server` に変更が含まれるため、`pm2 restart devrelay-server` が必要（本 devlog 作成時点では未実施）。
- DB マイグレーションは無し（キルスイッチ用の env 追加のみ、スキーマ変更なし）。
- push はサイクル完了時にまとめて実施する。

## 人間側 E2E（実施待ち、前サイクルからの引き継ぎ）

- E2E-1〜E2E-5（前サイクルの devlog `archive_worklog_2026-09.md` 記載分）は本 devlog の対象外。
  `pm2 restart` 後に別途確認が必要。
